node_modules/
dist/
# The workflow manager's build output: apps/ui compiles into the daemon that serves it.
packages/daemon/web/
*.tgz
.weft/runs/
.weft/tasks/
*.log
.DS_Store
coverage/
.turbo/
.weft/blobs/
.weft/index.sqlite
*.tsbuildinfo

# Local Playwright MCP artifacts and workflow manager prototype.
/.playwright-mcp/
/weft-workflow-manager-prototype/

# A scratch project generated by the todo-app workflow — its own git repo, not part of Weft.
/todo-app/
